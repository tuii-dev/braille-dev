import { fn } from "@storybook/test";

export const updateExtractedData = fn(() => {});

export const fetchEntityOptions = fn(() => {});

export const executeAction = fn(() => {});

export const rerunJob = fn(() => {});
